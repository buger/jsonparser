id: V-panic-negative-array-index
title: Negative array index stale-offset panic sweep
description: Hunt for panics from negative array index paths (e.g. [-1]) parsed via strconv.Atoi with no sign check. The curIdx match never fires and stale valueOffset arithmetic can panic on stale-offset dereference. Covered by FuzzGetNative in fuzz_native_test.go and negative-index regression tests in parser_test.go.
priority: P0
status: closed-null
related_obligation_classes:
    - negative_array_index
stacks:
    - go
close_note: Hunted. Worst case (negative array index path like [-1] parsed via strconv.Atoi with no sign check -> curIdx never matches -> stale valueOffset dereference panics) covered by FuzzTokenStartNative (fuzz_native_test.go) and negative-index regression in parser_test.go. Campaign saturated, 0 crashes on this class. No NEW Med+.
research:
    campaign: run_fuzz_campaign.sh (4 passes x 180s/target) driving the Fuzz*Native corpus harnesses, plus targeted regression tests.
    evidence:
        - FuzzTokenStartNative in fuzz_native_test.go drives the Get/array-index path over libFuzzer-generated bytes (the OSS-Fuzz surface).
        - Negative-index regression tests in parser_test.go pin the post-fix not-found outcome for paths like [-1].
    finding: Worst case is a negative array index (e.g. [-1]) parsed via strconv.Atoi with no sign check, so curIdx never matches and stale valueOffset arithmetic can panic on a stale-offset dereference. Negative indices resolve to not-found rather than panicking. Campaign saturated with zero crashes on this class after the fix; no NEW Med+ product defect.
campaign_log:
    - at: "2026-07-26T14:57:23Z"
      status: closed-null
      note: Hunted. Worst case (negative array index path like [-1] parsed via strconv.Atoi with no sign check -> curIdx never matches -> stale valueOffset dereference panics) covered by FuzzTokenStartNative (fuzz_native_test.go) and negative-index regression in parser_test.go. Campaign saturated, 0 crashes on this class. No NEW Med+.
      actor: agent
